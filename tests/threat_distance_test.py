__author__ = 'drock'

from source.GameObject import *
from unittest import TestCase

class TestThreatDistance(UnitTest)
    def test_thread_distance(self):
        orc = Orc(5)
        myPosition = Position(-2)
        distance = Orc.getDistance(myPosition)